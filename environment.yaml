name: expert-system-gpt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - kedro=0.18.4
  - pip:
      - -r expert-system-gpt-pipeline/src/requirements.txt
prefix: /usr/local/anaconda3/envs/expert-system-gpt
